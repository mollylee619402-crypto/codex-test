# codex-test

一个用于练习协作开发与工程化流程的示例仓库。

## 1. 项目简介

本项目当前处于初始化阶段，目标是为新同学提供一个“从 0 到 1”熟悉代码库的实践环境。你可以在这里学习：

- 如何阅读与维护项目文档。
- 如何搭建基础目录结构与开发流程。
- 如何通过小步提交推进功能开发。

> 当前仓库仍是最小版本，后续会逐步补充代码、测试和自动化配置。

---

## 2. 目录结构

当前目录较精简，建议按如下结构逐步演进：

```text
codex-test/
├── README.md                # 项目说明（你正在阅读）
├── src/                     # 源代码目录（后续新增）
├── tests/                   # 测试目录（后续新增）
├── docs/                    # 设计文档与规范（后续新增）
└── scripts/                 # 开发/发布脚本（后续新增）
```

目录说明建议：

- `src/`：放核心业务代码。
- `tests/`：放单元测试、集成测试。
- `docs/`：放架构设计、开发规范、FAQ。
- `scripts/`：放一键运行、检查、发布相关脚本。

---

## 3. 如何开始

### 3.1 克隆仓库

```bash
git clone <your-repo-url>
cd codex-test
```

### 3.2 创建开发分支

```bash
git checkout -b feat/your-topic
```

### 3.3 开发建议流程

1. 先更新/补充文档（README、docs）。
2. 再新增最小可运行代码（MVP）。
3. 补充测试与检查脚本。
4. 提交变更并发起 PR。

### 3.4 提交示例

```bash
git add .
git commit -m "docs: initialize beginner-friendly README"
```

---

## 4. 后续开发计划

建议按以下优先级推进：

### P0（基础建设）

- [ ] 明确技术栈（语言、框架、包管理工具）。
- [ ] 建立 `src/` 与 `tests/` 基础骨架。
- [ ] 添加格式化和静态检查工具。
- [ ] 完善快速启动说明。

### P1（开发规范）

- [ ] 增加 `CONTRIBUTING.md`（提交流程、分支策略、Review 规范）。
- [ ] 增加 PR 模板与 Issue 模板。
- [ ] 编写 `docs/architecture.md` 描述模块边界。

### P2（质量与自动化）

- [ ] 建立 CI（自动 lint / test）。
- [ ] 补齐关键路径测试样例。
- [ ] 增加发布或部署脚本（如适用）。

---

## 5. 给 Codex 使用的注意事项

当你使用 Codex 协助本仓库开发时，建议遵循：

1. **先读文档再改代码**
   - 优先让 Codex 阅读 `README.md` 和 `docs/`，确认项目约束后再修改。

2. **一次只做一类改动**
   - 一个任务聚焦一件事（例如“仅补 README”或“仅新增测试”），便于审查与回滚。

3. **要求输出变更摘要**
   - 让 Codex 在结果中写清：改了哪些文件、为什么改、如何验证。

4. **要求执行最小验证命令**
   - 例如：格式检查、测试命令、构建命令。

5. **避免高风险批量重构**
   - 在项目早期，优先小步快跑；大改动应先提交设计说明。

6. **提交前人工复核**
   - 尤其关注：删除文件、依赖升级、脚本改动、配置文件变更。

---

如果你是第一次接触这个仓库，建议从“补齐目录 + 添加第一个可运行示例 + 写一个最小测试”开始。


## 6. 文本/CSV 自动整理工具使用说明

### 6.1 工具用途

“文本/CSV 自动整理工具”用于快速整理多行文本或 CSV 内容，适合做名单清洗、行级去重、批量排序、导出标准 CSV 等日常办公场景。

### 6.2 在线访问地址

- 在线访问：`https://mollylee619402-crypto.github.io/codex-test/`
- 本地访问：直接在浏览器打开仓库根目录下的 `index.html`。

### 6.3 功能列表

当前支持以下功能：

- 清理每行前后空格
- 去除空行
- 去除重复行（保留首次出现顺序）
- 按行排序
- 一键复制结果
- 上传 CSV 文件并读取到文本框
- 下载为 CSV 文件（`cleaned.csv`）
- 清空内容

### 6.4 使用步骤

1. 打开工具页面（在线地址或本地 `index.html`）。
2. 将多行文本/CSV 粘贴到文本框，或点击“上传 CSV 文件”导入本地 `.csv` 文件。
3. 按需点击“清理前后空格 / 去除空行 / 去除重复行 / 按行排序”等按钮进行处理。
4. 处理后可点击“一键复制结果”直接粘贴到其他系统。
5. 如需文件输出，点击“下载为 CSV 文件”，导出 `cleaned.csv`。
6. 如需重新开始，可点击“清空内容”。

### 6.5 数据隐私说明

所有处理均在浏览器本地完成，不会上传文本或 CSV 数据到服务器。

### 6.6 常见问题（FAQ）

**Q：Excel 打开 CSV 出现中文乱码怎么办？**

A：请优先使用工具导出的 `cleaned.csv`（已包含 UTF-8 BOM，通常可被 Excel 正确识别中文）。如果仍有乱码，建议在 Excel 中通过“数据 -> 自文本/CSV 导入”，并在导入时手动选择编码为 UTF-8。


## 7. Python Excel 项目周报自动汇总工具

### 7.1 功能简介

本仓库提供 `weekly_project_report_merge.py`，用于自动读取 `input/` 目录下多个 `.xlsx` 周报文件，汇总所有工作表中的项目记录，并输出到 `output/weekly_project_summary.xlsx`。

工具特性：

- 自动扫描 `input/` 下全部 `.xlsx` 文件。
- 自动处理合并单元格（将合并区域左上角值填充到区域内每个单元格）。
- 自动识别表头行（不要求表头在第 1 行）。
- 支持字段别名匹配。
- 跳过完全空白的行。
- 保留中文与单元格中的换行内容。
- 输出带基础样式（表头加粗、自动换行、列宽、冻结首行）。

### 7.2 目录约定

```text
codex-test/
├── weekly_project_report_merge.py
├── requirements.txt
├── input/                    # 放待汇总的 .xlsx 周报
│   └── .gitkeep
└── output/
    ├── weekly_project_summary.xlsx
    └── .gitkeep
```

### 7.3 安装依赖

```bash
pip install -r requirements.txt
```

### 7.4 使用方法

1. 将周报 Excel 文件（`.xlsx`）放入 `input/` 目录。
2. 运行命令：

```bash
python weekly_project_report_merge.py
```

3. 运行完成后，在 `output/weekly_project_summary.xlsx` 查看汇总结果。

### 7.5 表头识别与别名说明

脚本会自动识别包含核心关键词的表头行（例如：`项目名称`、`本周工作进展`、`下一步工作`）。

已支持的别名示例：

- `项目名称`：可匹配 `项目名`
- `本周工作进展`：可匹配 `本周进展`、`本周工作`
- `下一步工作时间节点`：可匹配 `时间节点`、`下一步时间`

此外还支持常见扩展别名（如 `执行人` -> `执行人员`）。

### 7.6 输出字段

输出文件表头固定为：

- 来源文件
- 来源工作表
- 序号
- 项目名称
- 项目概况
- 执行人员
- 项目进展情况
- 本周工作进展
- 下一步工作
- 下一步工作时间节点

### 7.7 终端统计信息

脚本结束后会打印：

- 读取了多少个 Excel 文件
- 读取了多少个工作表
- 成功汇总了多少条项目记录
- 跳过了多少空行
- 输出文件路径

## 8. 项目资料清单生成工具

### 8.1 功能简介

本仓库提供 `project_file_inventory.py`，用于新项目资料整理的第一步：递归扫描 `input/` 目录及其所有子目录，并自动生成 Excel 文件清单 `output/project_file_inventory.xlsx`。

工具特性：

- 自动创建 `input/` 和 `output/` 目录。
- 递归扫描 `input/` 下的所有文件。
- 根据扩展名自动识别常见文件类型。
- 自动忽略 `.gitkeep`、`.DS_Store`、`Thumbs.db` 等系统或占位文件。
- 输出 Excel 清单，包含基础样式、冻结首行、自动筛选和合理列宽。
- 运行完成后在终端打印扫描文件数量和输出文件路径。

### 8.2 安装依赖

```bash
pip install -r requirements.txt
```

依赖说明：

- `openpyxl`：用于生成和设置 Excel 文件样式。

### 8.3 使用方法

1. 将需要整理的项目资料放入 `input/` 目录，可以按项目、专业或资料类别继续建立子文件夹。
2. 运行命令：

```bash
python project_file_inventory.py
```

3. 运行完成后，在 `output/project_file_inventory.xlsx` 查看资料清单。

### 8.4 输出字段说明

生成的 Excel 文件包含以下字段：

| 字段 | 说明 |
| --- | --- |
| 序号 | 文件序号，从 1 开始递增 |
| 文件名 | 文件名称，包含扩展名 |
| 扩展名 | 文件扩展名，例如 `.pdf`、`.docx` |
| 文件类型 | 根据扩展名识别出的文件类型 |
| 所在文件夹 | 文件所在文件夹的完整路径 |
| 完整路径 | 文件的完整路径 |
| 文件大小KB | 文件大小，单位 KB |
| 修改时间 | 文件最后修改时间 |
| 备注 | 预留人工补充说明 |

### 8.5 文件类型识别规则

| 扩展名 | 文件类型 |
| --- | --- |
| `.doc` / `.docx` | Word 文档 |
| `.xls` / `.xlsx` | Excel 表格 |
| `.ppt` / `.pptx` | PowerPoint 演示文稿 |
| `.pdf` | PDF 文档 |
| `.dwg` | CAD 图纸 |
| `.dxf` | DXF 图纸 |
| `.csv` | CSV 表格 |
| `.txt` | 文本文件 |
| `.jpg` / `.jpeg` / `.png` | 图片 |
| `.zip` / `.rar` / `.7z` | 压缩包 |
| 其他扩展名 | 其他文件 |

### 8.6 忽略文件规则

为避免把系统文件或目录占位文件写入资料清单，脚本会自动跳过以下文件：

- `.gitkeep`
- `.DS_Store`
- `Thumbs.db`
